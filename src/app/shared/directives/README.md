# Shared — Directives

Directive ที่ใช้ร่วมกัน

| File | Description |
|------|-------------|
| `has-role.directive.ts` | `*appHasRole="'admin'"` — แสดง/ซ่อนตามบทบาทผู้ใช้ |
| `has-permission.directive.ts` | `*appHasPermission="'users.create'"` — ตรวจสอบ permission |