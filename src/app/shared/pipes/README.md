# Shared — Pipes

Pipe ที่ใช้ร่วมกัน

| File | Description |
|------|-------------|
| `status-label.pipe.ts` | `statusLabel` — แปลง enum เป็นข้อความภาษาไทย |
| `thai-currency.pipe.ts` | `thaiCurrency` — แปลง number → "฿xxx,xxx.xx" |
| `thai-date.pipe.ts` | `thaiDate` — แปลง Date → "31 ม.ค. 2568" |