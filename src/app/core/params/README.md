# Core — Params

Wrapper classes สำหรับส่ง parameter เข้า use case

| File | Description |
|------|-------------|
| `param.payload.ts` | `Param<T>` — generic wrapper |
| `no-param.paylod.ts` | `NoParam` — เมื่อ use case ไม่ต้องการ parameter |