# Core — Utils

ฟังก์ชันช่วยเหลือสำหรับงานทั่วไป

| File | Description |
|------|-------------|
| `helpers.ts` | `generateId`, `debounce`, `truncate`, `getStatusLabel` (ภาษาไทย) |
| `validators.ts` | `AppValidators` — email, phone, passwordStrength, match validators |
| `formatters.ts` | `Formatters` — currency (THB), date (ไทย), phone, fileSize |