# Core — Contracts

Interfaces และ abstract classes สำหรับกำหนดรูปแบบมาตรฐาน

| File | Description |
|------|-------------|
| `usecase.contract.ts` | `Usecase<T, R>` interface — Command Pattern (param → result) |
| `mapper.contract.ts` | `Mapper<E, R>` abstract class — Entity ↔ DTO mapping |