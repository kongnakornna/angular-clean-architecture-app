# Layouts — Classic

เลย์เอาต์หลักของระบบ — sidebar + navbar + content area

## Components

| Component | Description |
|-----------|-------------|
| `ClassicComponent` | Wrapper หลัก — ประกอบด้วย Navbar + Sidebar + Router Outlet |

## Dependencies

- Navbar (shared)
- Sidebar (shared)
- Bootstrap / Tabler CSS